export default {
    setup() {console.log('h')},
    respond(action, index) {
     }
}

// You can then insert this into the DOM using innerHTML or insertAdjacentHTML()
//document.getElementById('custom').innerHTML = htmlContent;
// or
//document.getElementById('targetDiv').insertAdjacentHTML('beforeend', htmlContent);

